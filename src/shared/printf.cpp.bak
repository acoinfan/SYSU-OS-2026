#include "printf.h"
#include "vsprintf.h"
#include "os_modules.h"

static void stdio_putc(char c) {
    stdio.print(c);
}

int printf(const char *const fmt, ...) {
    va_list ap;
    va_start(ap, fmt);
    int ret = vsprintf_callback(stdio_putc, fmt, ap);
    va_end(ap);
    return ret;
}
